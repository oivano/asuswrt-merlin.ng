# Section with settings specific to the CLI client `charon-cmd`. Settings from
# the `charon` section are not inherited, but many can be used here as well.
charon-cmd {

    # Load only explicitly enabled plugins
    load_modular = yes

    plugins {

        # Plugin config snippets used by charon-cmd
        include charon-cmd/*.conf

    }

}

